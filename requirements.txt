# Consolidated dependencies for BOTH services (pipeline + dashboard). Both
# Railway services now build from the repo root (see README.md section 3),
# so there is a single requirements.txt instead of one per subfolder.
fastapi==0.115.0
uvicorn[standard]==0.30.6
jinja2==3.1.4
python-multipart==0.0.9
itsdangerous==2.2.0
supabase==2.9.1
google-api-python-client==2.149.0
google-auth==2.35.0
google-auth-httplib2==0.2.0
pdfplumber==0.11.4
openpyxl==3.1.5
anthropic==0.39.0
